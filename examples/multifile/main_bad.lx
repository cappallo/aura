module multifile.main_bad

import multifile.math

fn compute(x: Int) -> Int {
  let doubled = math.add(x, x)
  return math.multiply(doubled, "oops")
}

test basic_import {
  let result = compute(5)
  test.assert_equal(result, 30)
}
